name: Create Board

on:
  push:
  workflow_dispatch:

jobs:
  call-workflow-passing-data:
    uses: chuckwagoncomputing/hellen-one/.github/workflows/create-board.yaml@kicad-8
    permissions:
      contents: write
